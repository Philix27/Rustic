// import { AllMcq } from "@/lib";
import React from "react";

export default function ContentArticle(props: { title: string }) {
  // const dataList = AllMcq.filter((v, index) => v.title === props.title)[0];
  return (
    <div>
      {/* {dataList.list.map((v, i) => (
        <div>
          <p>{v.answer_index} </p>
          <p>{v.question} </p>
        </div>
      ))} */}
    </div>
  );
}
